@using website.Data

@if (Project != null)
{
    <div class="project-card-container">
        <div class="project-card bg-gray-800 rounded-lg overflow-hidden shadow-xl transition-all duration-500 border border-transparent hover:border-cyan-500/50 relative">
            @* Diagonal "IN DEVELOPMENT" ribbon *@
            @if (Project.IsInDevelopment)
            {
                <div class="absolute top-0 right-0 z-10 overflow-hidden w-32 h-32 pointer-events-none">
                    <div class="absolute transform rotate-45 bg-gradient-to-r from-yellow-500 to-orange-500 text-white text-xs font-bold py-1 right-[-35px] top-[32px] w-[170px] text-center shadow-lg">
                        IN DEVELOPMENT
                    </div>
                </div>
            }

            @if (!string.IsNullOrEmpty(Project.ImageUrl))
            {
                <img src="@Project.ImageUrl" alt="@(Project.Title ?? "Project")" class="w-full h-48 object-cover">
            }
            else
            {
                <div class="w-full h-48 bg-gray-700 flex items-center justify-center">
                    <i class="fas fa-code text-gray-500 text-4xl"></i>
                </div>
            }
            <div class="p-6">
                <h3 class="text-2xl font-bold text-white mb-2">@(Project.Title ?? "Untitled Project")</h3>
                <p class="text-gray-400 mb-4">@(Project.Description ?? "No description available")</p>
                @if (Project.Tags != null && Project.Tags.Any())
                {
                    <div class="flex flex-wrap gap-2 mb-4">
                        @foreach(var tag in Project.Tags)
                        {
                            @if (!string.IsNullOrEmpty(tag))
                            {
                                <span class="bg-gray-700 text-cyan-400 text-xs font-semibold px-2.5 py-1 rounded-full">@tag</span>
                            }
                        }
                    </div>
                }
                @if (!string.IsNullOrEmpty(Project.ProjectUrl))
                {
                    <a href="@Project.ProjectUrl" class="inline-block mt-2 font-bold text-cyan-400 hover:text-white transition-colors duration-300">
                        View Project &rarr;
                    </a>
                }
            </div>
        </div>
    </div>
}
else
{
    <div class="bg-gray-800 rounded-lg p-6 border border-red-500/50">
        <p class="text-red-400">Error: Project data not available</p>
    </div>
}

@code {
    [Parameter]
    public Project Project { get; set; } = new();
}